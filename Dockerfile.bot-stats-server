FROM node:22-alpine

WORKDIR /app

# 依存関係をインストール
COPY package*.json ./
RUN npm install

# アプリのソースと静的ファイルをコピー
COPY . .

# ポートを公開
EXPOSE 3000

# bot-stats-server.jsを起動
CMD ["node", "bot-stats-server.js"]
